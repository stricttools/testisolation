# DO NOT EDIT -- generated by rlsbl monorepo sync
name: CI Router
on:
  push:
    branches:
    - main
  pull_request:
  workflow_dispatch:
    inputs:
      run_all:
        description: Run every project's CI jobs, ignoring the paths filter. Use
          this to make a release candidate's members run their own CI on a 
          commit whose push window does not touch their paths.
        type: boolean
        required: false
        default: false
concurrency:
  group: ${{ github.workflow_ref }}-${{ github.sha }}-${{ inputs.run_all }}
  cancel-in-progress: true
jobs:
  detect:
    runs-on: ubuntu-latest
    outputs:
      stricttest: ${{ steps.changes.outputs.stricttest }}
      go-stricttest: ${{ steps.changes.outputs.go-stricttest }}
      ts-stricttest: ${{ steps.changes.outputs.ts-stricttest }}
    steps:
    - uses: actions/checkout@v6
    - uses: dorny/paths-filter@v4
      id: changes
      with:
        filters: |
          stricttest:
            - 'python/**'
            - '.github/workflows/ci-router.yml'
            - '.rlsbl-monorepo/releasables/py-testisolation/CHANGELOG.md'
          go-stricttest:
            - 'go/**'
            - '.github/workflows/ci-router.yml'
            - '.rlsbl-monorepo/releasables/go-testisolation/CHANGELOG.md'
          ts-stricttest:
            - 'typescript/**'
            - '.github/workflows/ci-router.yml'
            - '.rlsbl-monorepo/releasables/ts-testisolation/CHANGELOG.md'
        predicate-quantifier: some-with-excludes
  stricttest-ci-test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        # requires-python: >= 3.11
        python-version: ['3.12', '3.13', '3.14']
    steps:
    - uses: actions/checkout@v6
    - uses: astral-sh/setup-uv@v7
    - run: uv python install ${{ matrix.python-version }}
    - run: uv sync --locked
    - run: uv run python -c "import stricttest"
    - name: Install gitleaks
      run: |
        GITLEAKS_VERSION=8.24.3
        curl -sSfL "https://github.com/gitleaks/gitleaks/releases/download/v${GITLEAKS_VERSION}/gitleaks_${GITLEAKS_VERSION}_linux_x64.tar.gz" | tar xz -C /usr/local/bin gitleaks
    - run: uv run pytest --rootdir .
    defaults:
      run:
        working-directory: python
    name: stricttest-ci / test
    needs:
    - detect
    if: (needs.detect.outputs.stricttest == 'true' || inputs.run_all)
  go-stricttest-ci-test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6
      # go.mod: go 1.25.0
    - uses: actions/setup-go@v6
      with:
        go-version-file: go/go.mod
    - run: go vet ./...
    - run: go test ./... -race -short -timeout=10m
    defaults:
      run:
        working-directory: go
    name: go-stricttest-ci / test
    needs:
    - detect
    if: (needs.detect.outputs.go-stricttest == 'true' || inputs.run_all)
  ts-stricttest-ci-test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        # engines.node: >= 22 -- the scaffold's default matrix still lists 20,
        # which this package genuinely cannot run on: `node --test` gained glob
        # support in 21, and the test script passes one.
        node-version: [22, 24]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-node@v6
      with:
        node-version: ${{ matrix.node-version }}
    - name: Install dependencies
      run: |
        if [ -f package-lock.json ]; then
          npm ci
        else
          npm install
        fi
    - run: npm test --if-present
    - run: npm audit --audit-level=moderate || true
    defaults:
      run:
        working-directory: typescript
    name: ts-stricttest-ci / test
    needs:
    - detect
    if: (needs.detect.outputs.ts-stricttest == 'true' || inputs.run_all)
