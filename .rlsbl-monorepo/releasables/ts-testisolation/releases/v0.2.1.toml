# Archived by rlsbl at release time. This release had no unreleased.toml
# (a batch member takes its metadata from the batch release file); the
# archive is written so later changelog regenerations keep the
# description and context.
# strictspec document version gate (do not remove)
format_version = 1
bump = "patch"
include = ["npm"]
exclude = []
description = "git's background maintenance is disabled in test isolation so it no longer races test cleanup."
candidate_sha = "c4ee7bf10a79e3346dd4b8900b524e5a7e7cf719"

[tree_hashes]
typescript = "172bb9b4adae198e1d1a9ca337f42d1f5dc495b7"
